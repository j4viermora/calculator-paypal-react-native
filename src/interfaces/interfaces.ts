export interface Form {
  montoParaEnviar: string;
  montoParaRecibir: string;
}

export interface iInitialResult {
    totalAverage: number,
    totalToSend: number,
    totalAverageSend: number,
    totalReceive: number,
  };

